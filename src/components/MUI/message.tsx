import * as React from "react";

export interface MesaageProps {
  message: string;
  color: string | number;
}

const Mesaage: React.SFC<MesaageProps> = () => {
  return <div></div>;
};

export default Mesaage;
